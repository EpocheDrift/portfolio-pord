# Caddy config (add to your Caddyfile on VPS):
#
# zayn.chaostudio.org {
#     reverse_proxy localhost:3010
# }

services:
  portfolio:
    image: ghcr.io/epochedrift/portfolio-pord:latest
    container_name: portfolio-pord
    restart: unless-stopped
    ports:
      - "3010:80"
